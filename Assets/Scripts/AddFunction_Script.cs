using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class sonia_Script : MonoBehaviour {
/*

public Queue<int> NoteQueue;

public void songClip(){

	int length = GameMaster.Instance.getSongNote().getSongLength();
	
	int [] NewArray = new int [length];
	for( int i = 0; i < length; i++)
	{
		NewArray [i] = GameMaster.Instance.getSongNote().getSongNote(i);
	}

		for(i = 0; i < length; i++)
		{
			int index = Random.Range(0, length);
			
			tmp = NewArray[i];
			NewArray[i] = NewArray[index];
			NewArray[index] = tmp;
		}	

NoteQueue = new Queue<int>();
		for(i = 0; i < length; i++)
		{
			NoteQueue.Enqueque(NewArray(i));
		}
	}
	int getNextCard(){
		return NoteQueue.Dequeque();
	}
	void BackCard(int i){
		NoteQueue.Enqueque(i);
	}

    */
}
